.nuxt
.output
node_modules
.env
data/*.sqlite*
.DS_Store

# IDE / tool 产物（不入仓）
.cursor/
.codex_spreadsheet_analysis/

# 测试 / 烟囱 / 临时产物
coverage/
.tmp_wharttest/
# 一次性调试烟囱（推送/npm/任意命令的 stdout/stderr 旁路输出）
.tmp_*.out
.tmp_*.err
# 一次性分析/审计（Mavis 跑 coverage 解析、脚本调试的目录）
.tmp_audit/

# 一次性问题定位报告（不入仓）
/CAP_Agent_问题定位报告_*.html

# 一次性调试 stdout/stderr（vitest/coverage 烟囱 + scope 评估目录；不重写 coverage/）
.tmp_*.log
vitest-*.log
coverage_fresh_*
